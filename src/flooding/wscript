## -*- Mode: python; py-indent-offset: 4; indent-tabs-mode: nil; coding: utf-8; -*-

def build(bld):
    module = bld.create_ns3_module('flooding', ['internet', 'wifi'])
    module.includes = '.'
    module.source = [
        #'model/aodv-id-cache.cc',
        #'model/aodv-dpd.cc',
        #'model/aodv-rtable.cc',
        #'model/aodv-rqueue.cc',
        'model/flooding-packet.cc',
        #'model/flooding-neighbor.cc',
        'model/flooding-routing-protocol.cc',
        'helper/flooding-helper.cc',
        ]


    headers = bld(features='ns3header')
    headers.module = 'flooding'
    headers.source = [
        #'model/aodv-id-cache.h',
        #'model/aodv-dpd.h',
        #'model/aodv-rtable.h',
        #'model/aodv-rqueue.h',
        'model/flooding-packet.h',
        #'model/flooding-neighbor.h',
        'model/flooding-routing-protocol.h',
        'helper/flooding-helper.h',
        ]

    if bld.env['ENABLE_EXAMPLES']:
        bld.recurse('examples')

    bld.ns3_python_bindings()
